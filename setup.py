from setuptools import setup, find_packages

setup(
    name="gpt_baraa",  # قم بتسمية المشروع
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # ضع المتطلبات هنا أو اتركها فارغة إذا كانت مذكورة في requirements.txt
    ],
)
